// Em programação o uso de variáveis é fundamental, pois com ela guardamos valores que são utilizados e alterados ao longo do programa. Em 
// javascript existem três formas de declarar variáveis. Quais são? E quais características cada forma tem?

// São três formas de declarar variáveis:

//Constantes: Os valores de uma constante não pode ser mais alterado depois de declarada.
const constantePI = 3.1413;

//Variável var: Forma arcaica de declarar uma variável, permite que a mesma seja utilizada antes de sua declaração, como mostra o exemplo abaixo, em que o console log print
//undefined

console.log(x);

var x = 0;

//Variável let: forma recomendada para declarar uma variável, permite que a mesma seja utilizada somente após sua declaração, como mostra o exemplo abaixo:

let y = 1;

console.log(y);